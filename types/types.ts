export type authInputType = {
  name: string;
  type: string;
  placeholder?: string;
};
export type userType = {
  username: string;
  firstName: string;
  lastName: string;
  email: string;
  boolean: string;
};
